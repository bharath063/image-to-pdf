[project]
name = "img-to-pdf"
version = "0.3.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [ "pillow>=12.1.0", "pyqt6>=6.10.2",]

[project.optional-dependencies]
dev = [ "pyinstaller>=6.0.0",]

[tool.setuptools]
py-modules = ["main"]
